using UnityEngine;

public class VariableBehaviour : MonoBehaviour
{
    public float baseLiking = 0.5f;
    public float positionMultiplier = 0.5f;
    public float likingMultiplier = 0.5f;
    public float conspiracyThreshold = -0.5f;
    public float conspiracyMultiplier = 0.5f;
    public float likingBias = 0.0f;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
